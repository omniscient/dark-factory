{"project": "markethawk", "type": "avoidance", "status": "active", "source": "refine", "agent_id": "refine", "phase": "refine", "issue_number": 180, "files": [""], "scope": "dark-factory", "content": "This repo's working tree carries untracked runtime fallback-copy directories (dark-factory/, .claude/, .archon/commands/, .archon/workflows/) that mirror the canonical git-tracked top-level dirs (scripts/, refinement-skills/, commands/, workflows/) byte-for-byte \u2014 entrypoint.sh materializes them per-run and git-excludes them. Always read/edit the canonical top-level path; the mirrored copies are not committed and edits there are silently lost. Verify with 'git ls-files <dir>/' before trusting a path is source-of-truth.", "created_at": "2026-07-07", "expires_at": "2027-01-07"}
{"project": "markethawk", "type": "avoidance", "status": "active", "source": "refine", "agent_id": "refine", "phase": "refine", "issue_number": 180, "files": ["scripts/factory_core/"], "scope": "architecture", "content": "Keep .factory/adapter.yaml's safety.* block (target-repo-controlled: hard_exclude_paths, sensitive_keywords) fully separate from config/config.yaml's factory-owned blocks (e.g. epic_autopilot.hard_exclude_paths) \u2014 do not fold target-owned adapter input into effective_config.py's baked<-clone<-adapter merge just because both are 'config'. They are different trust boundaries (target vs factory controlled) that happen to share key names; conflating them let adapter.yaml silently shadow the factory's own self-improvement exclusion list in epic_autopilot.py (see #180 refinement, follow-up issue for the fix).", "created_at": "2026-07-07", "expires_at": "2027-01-07"}
