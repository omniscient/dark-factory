- [AVOID] For issue #18 (raise doc-slicing component-resolution hit-rate), scope was limited to adding an issue-title/body text inference signal (tiered: literal path-prefix match, then keyword-token match, reusing the existing _FILE_PREFIX_MAP/_SPEC_KEYWORD_MAP verbatim) rather than also implementing label-vocabulary enrichment or spec_component propagation. Reason: evals/token_opt_eval.py's eval_issue_scenario() never passes changed_files or spec_file/spec_component to the resolver for any scenario — only labels and the issue JSON (title/body) — so those other candidates cannot move the eval-measured hit-rate number without also rewriting the eval harness or re-labelling the historical corpus. Any future work on component resolution should check what signals evals/token_opt_eval.py actually exercises before assuming a code change will move its measured number. <!-- issue:#18 date:2026-07-06 expires:2027-01-06 source:refine agent:refine scope:architecture path:scripts/architecture_slice.py -->
