- [AVOID] When a refine/inventory ticket asks to compare N surfaces against a new analytical lens (role-card shape, loop-moves, anatomy gaps), scope the lens to the surfaces where it carries semantic meaning (e.g. actual LLM prompts) rather than running it across every deterministic-script/config row too — the latter produces mostly-null cells and quadruples the document for no signal. <!-- issue:#41 date:2026-07-10 expires:2027-01-10 source:refine agent:refine scope:architecture path:.archon/commands/ -->
- [AVOID] Do not author full new role-card/persona content as a side effect of a size:S inventory ticket — an inventory ticket's job is to identify candidates (source prompts + one-line rationale) and defer authoring to a follow-up implementation issue, especially when the pattern being adopted (e.g. agency-agents role cards) has zero prior references in the repo. <!-- issue:#41 date:2026-07-10 expires:2027-01-10 source:refine agent:refine scope:architecture path:.archon/commands/ -->
- [AVOID] When a refinement-prompt ticket is asked to consolidate a baked persona-prompt bundle (refinement-skills/ -> /opt/refinement-skills/) toward a clone-live Claude Skill layout, do not bundle the physical directory rename + reference updates (Dockerfile/entrypoint.sh/scheduler.sh/context_budget.py/etc.) with a same-ticket prose de-duplication pass, even if a sibling policy spec designed the target layout already. The rename is a baked-image-to-clone-live delivery-mechanism migration (different risk class, needs its own Docker/CI verification) and should stay its own follow-up ticket; only trim/merge prose in place at the current path. <!-- issue:#43 date:2026-07-10 expires:2027-01-10 source:refine agent:refine scope:architecture path:.archon/commands/ -->
- [AVOID] When an Archon phase command (clone-live, DAG-dispatched, e.g. dark-factory-refine.md) and a baked reference-skill prompt (e.g. refinement-skills/orchestrator-prompt.md) describe the same procedure in duplicate prose, the Archon command is always the canonical owner and the baked file shrinks to a thin persona stub - not the reverse. The dispatched, clone-live file already carries the real operational detail (scope boundary, memory rules, model pins, publish templates); do not migrate that detail into the baked file even if it originated there. Keep the stub file present (not deleted) if any script enumerates it by filename for token-budget accounting (e.g. context_budget.py's _SKILL_PROMPT_FILES). <!-- issue:#43 date:2026-07-10 expires:2027-01-10 source:refine agent:refine scope:architecture path:.archon/commands/ -->
- [AVOID] When migrating a Python module (not bash/YAML) off direct gh subprocess calls onto the factory_core provider layer, import and call get_tracker()/get_codehost() in-process — do not shell out to the new python -m factory_core.tracker/codehost CLI. The CLI exists so bash/YAML callers (which cannot import Python objects) can reach the provider layer; a Python module spawning a subprocess to run a sibling Python CLI is the exact indirection that split is designed to avoid. board.py already established this pattern (direct delegation to GitHubTracker); epic_autopilot.py/breaker.py/rescue.py/main_red_fixer.py follow it identically. <!-- issue:#249 date:2026-07-11 expires:2027-01-11 source:refine agent:refine scope:architecture path:scripts/factory_core/ -->
- [AVOID] When a call site's current behavior (e.g. smoke_gate.sh's atomic 'gh issue close --comment') has no clean match in an already spec-approved, frozen ABC from a prerequisite ticket (e.g. #248's Tracker.resolve_item(id), no comment param), do not split the call into multiple provider-CLI calls to force a fit — that changes the emitted argv count and breaks golden-argv parity tests, and misuses the existing method's documented semantics. Leave the call site inline with a TODO annotation and file a follow-up ticket proposing an ABC extension; do not extend a sibling ticket's already-approved contract unilaterally. <!-- issue:#249 date:2026-07-11 expires:2027-01-11 source:refine agent:refine scope:architecture path:scripts/factory_core/providers/ -->
