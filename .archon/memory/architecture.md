- [AVOID] Dark Factory phase commands (commands/*.md -> .archon/commands/*.md, dispatched by command: id in .archon/workflows/archon-dark-factory.yaml) must NOT be migrated to .claude/skills/<name>/SKILL.md with disable-model-invocation:true as a way to adopt Claude Skills conventions. With model-invocation disabled the SKILL.md never functions as a skill -- it becomes a renamed command file with a second loader path to maintain, risks fighting check_workflow_dag.py's command: id validation, and is pure format churn on the safety-critical implement/merge/close/deploy path for zero behavioral benefit. See docs/superpowers/specs/2026-07-10-dark-factory-claude-skills-design.md (issue #42). <!-- issue:#42 date:2026-07-10 expires:2027-01-10 source:refine agent:refine scope:architecture path:.archon/commands/ -->
- [AVOID] Dark Factory Claude Skills taxonomy: phase commands stay Archon-DAG-dispatched and are explicitly NOT Claude Code Skills (never model-invoked, no Skill frontmatter) -- this structurally guarantees implement/merge/close/deploy actions can't be model-auto-triggered. Real .claude/skills/<name>/SKILL.md is a separate 'reference skill' category (shared personas, lookup docs); any side-effecting reference skill requires disable-model-invocation:true by default (narrow, justified+human-signed-off exceptions only). See docs/superpowers/specs/2026-07-10-dark-factory-claude-skills-design.md (issue #42). <!-- issue:#42 date:2026-07-10 expires:2027-01-10 source:refine agent:refine scope:architecture path:.claude/skills/ -->
- [AVOID] Do not grant Dark Factory reference skills family-level Bash wildcards (Bash(git:*), Bash(gh:*)) even though they look safer than bare Bash(*) -- a family grant silently includes destructive/side-effecting subcommands (gh pr merge, gh release create) that must stay behind the phase-command boundary. Use exact-subcommand allowlists tiered by side-effect level instead (e.g. Bash(gh issue comment:*), Bash(python3 scripts/*.py:*)), allowed-tools only, no disallowed-tools. See docs/superpowers/specs/2026-07-10-dark-factory-claude-skills-design.md (issue #42). <!-- issue:#42 date:2026-07-10 expires:2027-01-10 source:refine agent:refine scope:architecture path:.claude/skills/ -->
- [AVOID] Do not retrofit allowed-tools frontmatter onto the existing 7 Archon phase commands as part of adopting Claude Skills conventions -- they carry no Skill frontmatter today and implicitly use a wide real tool surface (gh, git, python3 compact-artifact scripts, bash helper scripts); retrofitting is a behavior change the preserve-existing-behavior epic constraint forbids and belongs to a dedicated migration (see issue #41) preceded by a full usage audit, not folded into the skills-policy ticket. allowed-tools rules apply prospectively to new/consolidated reference skills only. See docs/superpowers/specs/2026-07-10-dark-factory-claude-skills-design.md (issue #42). <!-- issue:#42 date:2026-07-10 expires:2027-01-10 source:refine agent:refine scope:architecture path:.claude/skills/ -->
