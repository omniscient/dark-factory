- [AVOID] Architecture-slicing (architecture_slice.slice_architecture) is telemetry-only for every scenario today, including implement — it is invoked only from inside context_budget.py and context_pack.py (imported), never as a live script, and no commands/dark-factory-*.md file consumes its sliced output; each command's Phase 1 does a literal full Read of ARCHITECTURE.md/CLAUDE.md. Only memory top-k (load_memory_context.sh -> memory_retrieve.py) and diff ranking (diff_rank.py) are genuinely live-consumed in agent prompts today. Don't assume architecture_md being in a scenario's _SECTION_REGISTRY means it changes what the agent sees; check for an actual reviewer-prompt/command consumer before claiming a live effect. <!-- issue:#19 date:2026-07-06 expires:2027-01-06 source:refine agent:refine scope:architecture path:dark-factory/scripts/ -->
- [AVOID] Rejected adding a 'comment digest' section for conformance/code-review's token-optimization routing (issue #19): comment_digest.py is purpose-built around the factory-boundary-marker/continue-intent re-run cadence with no home in single-pass gates, neither scenario loads issue-comment content today (no existing baseline to optimize), and injecting raw comments into conformance specifically risks re-surfacing scope the spec deliberately excluded (fights its scope-enforcement mandate). Chosen instead: extend _SECTION_REGISTRY with architecture_md (telemetry/eval parity only) + memory_context (made genuinely live via the pre-provisioned validate/review PHASE_SOURCE_MAP keys in memory_retrieve.py). <!-- issue:#19 date:2026-07-06 expires:2027-01-06 source:refine agent:refine scope:architecture path:dark-factory/scripts/ -->
