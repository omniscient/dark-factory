- [AVOID] When a refine/inventory ticket asks to compare N surfaces against a new analytical lens (role-card shape, loop-moves, anatomy gaps), scope the lens to the surfaces where it carries semantic meaning (e.g. actual LLM prompts) rather than running it across every deterministic-script/config row too — the latter produces mostly-null cells and quadruples the document for no signal. <!-- issue:#41 date:2026-07-10 expires:2027-01-10 source:refine agent:refine scope:architecture path:.archon/commands/ -->
- [AVOID] Do not author full new role-card/persona content as a side effect of a size:S inventory ticket — an inventory ticket's job is to identify candidates (source prompts + one-line rationale) and defer authoring to a follow-up implementation issue, especially when the pattern being adopted (e.g. agency-agents role cards) has zero prior references in the repo. <!-- issue:#41 date:2026-07-10 expires:2027-01-10 source:refine agent:refine scope:architecture path:.archon/commands/ -->
- [AVOID] When a refinement-prompt ticket is asked to consolidate a baked persona-prompt bundle (refinement-skills/ -> /opt/refinement-skills/) toward a clone-live Claude Skill layout, do not bundle the physical directory rename + reference updates (Dockerfile/entrypoint.sh/scheduler.sh/context_budget.py/etc.) with a same-ticket prose de-duplication pass, even if a sibling policy spec designed the target layout already. The rename is a baked-image-to-clone-live delivery-mechanism migration (different risk class, needs its own Docker/CI verification) and should stay its own follow-up ticket; only trim/merge prose in place at the current path. <!-- issue:#43 date:2026-07-10 expires:2027-01-10 source:refine agent:refine scope:architecture path:.archon/commands/ -->
- [AVOID] When an Archon phase command (clone-live, DAG-dispatched, e.g. dark-factory-refine.md) and a baked reference-skill prompt (e.g. refinement-skills/orchestrator-prompt.md) describe the same procedure in duplicate prose, the Archon command is always the canonical owner and the baked file shrinks to a thin persona stub - not the reverse. The dispatched, clone-live file already carries the real operational detail (scope boundary, memory rules, model pins, publish templates); do not migrate that detail into the baked file even if it originated there. Keep the stub file present (not deleted) if any script enumerates it by filename for token-budget accounting (e.g. context_budget.py's _SKILL_PROMPT_FILES). <!-- issue:#43 date:2026-07-10 expires:2027-01-10 source:refine agent:refine scope:architecture path:.archon/commands/ -->
- [AVOID] When a 'roll out X modularization' ticket in the Claude-Skills epic family depends on a prior evaluation ticket whose scorecard clears no scenario for default-on/blocking invocation, scope the rollout to documenting+runtime-testing the wrapper for scenarios that already have a live skill artifact (Tier 1) only; do not build a first skill-modularized alternative or anticipatory wrapper/alias layer for a scenario the scorecard rates 'advisory-readiness'/confounded (Tier 2) — that is net-new gate-adjacent production surface the eval didn't clear, and belongs to a future ticket once a skill body exists. <!-- issue:#49 date:2026-07-13 expires:2027-01-13 source:refine agent:refine scope:architecture path:.archon/commands/ -->
- [AVOID] A rollout-status/runbook doc whose job is to record ongoing, mutable operational state (e.g. per-scenario tier/rollout state that changes as evals advance) should be authored as its own living spec kept permanently at docs/superpowers/specs/ (status line marked living, only the ticket's plan doc archived) rather than folded into an existing tested policy spec (couples churny operational state into a stable, content-asserting-test-guarded doc) or a brand-new top-level docs/ location (no repo precedent outside the docs/superpowers/specs convention for authored design/policy prose). <!-- issue:#49 date:2026-07-13 expires:2027-01-13 source:refine agent:refine scope:architecture path:.archon/commands/ -->
- [AVOID] When adding an HTTP-calling Tracker/CodeHost provider adapter (e.g. Jira, GitLab), use stdlib urllib.request behind a single internal request-helper seam -- do not add requests/httpx/vcrpy/responses. This repo has zero third-party HTTP or HTTP-mocking dependencies (no requirements.txt/pyproject.toml; Dockerfile pip-installs only codeindex/pre-commit/pyyaml); every existing HTTP call in scripts/factory_core/ already uses urllib.request. 'Recorded HTTP fixtures' in this repo means hand-rolled JSON fed through a monkeypatched request-helper (mirroring test_provider_tracker_parity.py's subprocess.run monkeypatch idiom), not a VCR cassette library. <!-- issue:#251 date:2026-07-13 expires:2027-01-13 source:refine agent:refine scope:architecture path:scripts/factory_core/providers/ -->
- [AVOID] The provider-abstraction design doc's 'shared Tracker/CodeHost contract suite' (docs/provider-abstraction-design.md section 10) does not exist yet as of the #251 refinement pass -- only an ABC-shape test (test_provider_tracker_base.py) and GitHub-only golden-argv parity tests exist. An adapter ticket whose acceptance criteria says 'passes the same contract suite as GitHubTracker' must itself author the missing shared, implementation-agnostic behavioral suite (parametrized over both implementations via each one's own I/O-monkeypatch seam), not just add its own fixture tests -- the narrower per-implementation reading under-delivers the acceptance criterion. <!-- issue:#251 date:2026-07-13 expires:2027-01-13 source:refine agent:refine scope:architecture path:scripts/factory_core/providers/ -->
- [AVOID] When a ticket 'Depends on' a sibling provider-abstraction step whose PR already exists but is stuck on needs-discussion and unmerged (e.g. #250's registry/_TRACKERS wiring), do not assume it will have landed by implementation time. Build the new adapter standalone against the current (pre-dependency) ABC shape on main, implement any forward-looking hook (e.g. required_env()) directly on the new class without touching the shared registry file, and defer wiring to the blocked ticket or a trivial follow-up -- touching the registry/ABC yourself collides with the in-flight PR and violates scope discipline. <!-- issue:#251 date:2026-07-13 expires:2027-01-13 source:refine agent:refine scope:architecture path:scripts/factory_core/providers/ -->
- [AVOID] Tracker.get_children()'s only real shape precedent is GitHubTracker's raw GraphQL envelope (state: OPEN/CLOSED, labels: {nodes:[{name}]}), silently baked into epic_autopilot.py's _sub_issue_numbers (its only caller, disabled by default). New Tracker adapters should return the adapter-neutral shape ({id, status: canonical, labels: [str]}) matching get_item()'s conventions, not fabricate GitHub's envelope -- epic_autopilot.py's GitHub-shape coupling is a known, separately-tracked gap (harmless while epic_autopilot stays off by default), not something new adapters should perpetuate. <!-- issue:#251 date:2026-07-13 expires:2027-01-13 source:refine agent:refine scope:architecture path:scripts/factory_core/ -->
- [AVOID] For branch-scope/OOS-gate diff detection, use three-dot merge-base semantics (git diff origin/main...HEAD), not raw two-dot (git diff origin/main HEAD) — two-dot conflates the branch's own commits with main's independent post-fork history and can misclassify/delete files the branch never touched. dark-factory-conformance.md, dark-factory-code-review.md, dark-factory-implement.md, and dark-factory-validate.md already use three-dot; oos_excise.sh was the one outlier (fixed in #266). <!-- issue:#266 date:2026-07-13 expires:2027-01-13 source:refine agent:refine scope:architecture path:.archon/commands/ -->
- [AVOID] When fixing a gate/script bug that a 'behavior parity' test class was written to protect (i.e. the test asserts the new code matches an old inline block byte-for-byte), delete that parity test rather than updating its embedded reference snippet to mirror the fix — the parity premise (extraction preserved behavior) is invalidated once the fix deliberately changes behavior, and keeping a hand-rolled duplicate of the algorithm produces a tautology test, not real coverage. Cover the fix with new behavioral tests against concrete fixture state instead (see oos_excise.sh TestOosExciseBehaviorParity removal, #266). <!-- issue:#266 date:2026-07-13 expires:2027-01-13 source:refine agent:refine scope:architecture path:.archon/commands/ -->
