- [AVOID] For scheduler.sh/entrypoint.sh hot-loop bash-to-Python consolidations (e.g. per-issue PR-lookup/check-bucket parsing called every poll cycle), a python3 factory_core/cli.py subprocess call per issue is acceptable and matches the existing board-move/rescue-blocked precedent -- do not build a batched once-per-cycle resolver unless profiling actually shows the poll cycle is too slow; there is no documented latency budget and gh itself already dominates the added interpreter-startup cost. <!-- issue:#181 date:2026-07-07 expires:2027-01-07 source:refine agent:refine scope:architecture path:scheduler.sh -->
- [AVOID] When collapsing a duplicated bash implementation into a factory_core/cli.py call (thin-adapter conversion), keep the bash function's existing name and give it a one-line python3 cli.py <subcommand> delegation body IF it still has other in-file callers -- this matches the set_board_status precedent and keeps every other call site in the file untouched. Only delete a function outright when the conversion removes its last caller (e.g. a lookup helper whose sole caller is itself becoming a cli.py delegation), otherwise it survives as a dead orphan wrapper. <!-- issue:#181 date:2026-07-07 expires:2027-01-07 source:refine agent:refine scope:architecture path:scripts/factory_core/ -->
- [AVOID] Acceptance criteria phrased as 'X appears only in Y' or 'exactly one implementation of Z' are global-absence invariants -- they are false if even one duplicate/raw call site remains anywhere in the repo, so refine specs cannot satisfy them by splitting the ticket across sequenced follow-up PRs (this repo also has no precedent for phase-splitting one issue's spec). Scope the full multi-file change into one PR, ordered internally (shared seam first, adapters second, dead-code deletion last) for reviewability; size:L + dispatch_ceiling's human-pairing gate already supplies the staged-rollout safety net a phase split would otherwise provide. Relevant to the 2026-07-06 architecture-deepening-review batch (issue #181 was candidate 2 of 6) where similarly-phrased ACs are likely to recur. <!-- issue:#181 date:2026-07-07 expires:2027-01-07 source:refine agent:refine scope:architecture path:docs/superpowers/specs/ -->
