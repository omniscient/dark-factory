# Codebase Patterns — Accumulated Lessons

This file is maintained automatically by the dark factory implement agent. Do not edit manually.
Entries are advisory. If an entry conflicts with CLAUDE.md or ARCHITECTURE.md, follow those documents.
- [INVALID: archive step must NOT rename the design spec into docs/archive/ when it is the ticket's living policy deliverable — only the plan] When a refine-phase spec/plan was approved on a sibling `refine/issue-N-...` branch, the implement phase must itself copy `docs/superpowers/specs/*.md` and `docs/superpowers/plans/*.md` onto the `feat/issue-N-...` branch and commit them — they do not transfer automatically, and a plan step that assumes the spec "is already committed" (e.g. for a doc-pinning regression test) will be testing a file that does not yet exist on the feat branch. Mirrors the #41 precedent (commits `52ce1a6`, `621155e`); a later archive step (out of scope for implement) renames them into `docs/archive/`. <!-- issue:#42 date:2026-07-10 expires:2027-01-10 source:implement -->
- [PATTERN] When a refine-phase spec/plan was approved on a sibling `refine/issue-N-...` branch, the implement phase must itself copy `docs/superpowers/specs/*.md` and `docs/superpowers/plans/*.md` onto the `feat/issue-N-...` branch and commit them — they do not transfer automatically. A later archive step must rename only the *plan* into `docs/archive/`; the design spec stays at its durable `docs/superpowers/specs/` path if it is the ticket's living policy/reference deliverable (not just a completed workflow artifact) — archiving both broke CI on PR #215 (test + README both pin the spec path). <!-- issue:#42 date:2026-07-10 expires:2027-01-10 source:implement -->


## Documentation Diagrams

- [PATTERN] Mermaid diagrams belong in the file that owns their facts (e.g. topology → `ARCHITECTURE.md`, domain model → `CONTEXT.md`); delete any standalone `docs/Diagram.md`-style orphan with `git rm` if its content duplicates an existing section. <!-- issue:#174 date:2026-06-04 expires:2026-12-04 source:implement -->

## Memory Context Loading

- [PATTERN] When building memory context for a subagent prompt, load only the files relevant to the component area being worked on (e.g. backend changes → `backend-patterns.md`; dark factory ops → `dark-factory-ops.md`). Loading all memory files unconditionally bloats the prompt and dilutes signal. The plan workflow's `$MEMORY_CONTEXT` bash block demonstrates the selective pattern. <!-- issue:#149 date:2026-06-02 expires:2026-12-02 source:implement -->

## Scope / Out-of-Scope Detection

- [PATTERN] Use `git diff origin/main HEAD -- <file>` (two-dot) to test whether a file is truly out-of-scope relative to main — if empty, main already carries the same content. The three-dot form (`git diff origin/main...HEAD`) includes commits that main merged independently after the branch diverged, producing false-positive OOS hits on files that are net-identical to main. <!-- issue:#250 date:2026-06-11 expires:2026-12-11 source:implement -->

- [AVOID] When spec coverage priority files yield less than the target threshold, do not silently add out-of-list files — document the deviation explicitly in the config comment block (why the assumption failed, which files were needed) so the deviation is classified as MINOR (documented/justified) rather than MATERIAL (silent scope expansion). <!-- issue:#250 date:2026-06-11 expires:2026-12-11 source:conformance path:frontend/ -->
- [AVOID] Full-pipeline regression tests must use the transaction-rollback db fixture from conftest.py (not MagicMock DB), which ensures SQLAlchemy queries actually execute against a real schema and triggers the SAVEPOINT-based isolation <!-- issue:#288 date:2026-06-12 expires:2026-12-12 source:conformance path:backend/tests/services/ -->
- [AVOID] time stop: exit at next session open (subsequent_bars[i+1].open), not current bar close — spec req #6 <!-- issue:#301 date:2026-06-13 expires:2026-12-13 source:conformance path:backend/app/services/ -->
- [AVOID] limit entry fill model: compute limit_price from signal_previous_close (indicators dict), not entry_bar.open <!-- issue:#301 date:2026-06-13 expires:2026-12-13 source:conformance path:backend/app/services/ -->
- [AVOID] missing strategy_snapshot: capture strategy fields as JSONB at run start for determinism; live reads break re-run reproducibility <!-- issue:#301 date:2026-06-13 expires:2026-12-13 source:conformance path:backend/app/models/ -->
- [AVOID] exit_date omission: always assign exit_date_val after _simulate_trade returns — unassigned date fields silently persist NULL <!-- issue:#301 date:2026-06-13 expires:2026-12-13 source:conformance path:backend/app/services/ -->
- [AVOID] Gating probe_running_postgres() behind an env-var (e.g. POSTGRES_DISCOVERY_ENABLED==true) in _testcontainers_url() is a fixture behaviour change that violates spec requirement #2; if a safety concern requires a gate, the spec must be updated to include it — do not add opt-in gates silently in the implementation <!-- issue:#360 date:2026-06-13 expires:2026-12-13 source:conformance path:backend/tests/ -->
- [AVOID] The probe falls back to well-known host `stockscanner-db` with database `stockscanner` — the actual dev/live stack (confirmed in docker-compose.yml). `db_engine` (session scope) calls `Base.metadata.create_all()` and `Base.metadata.drop_all()` on whatever URL the probe returns, so a test run in an environment where the real DB is reachable will DROP production/dev tables. Fix: restrict the probe to test-only databases (require a `TEST_DATABASE_URL` or a `_test`-suffixed dbname), or never run `drop_all`/`create_all` against a discovered (non-testcontainers) DB; at minimum exclude the live `stockscanner` db/host from `common_creds`/hostnames. <!-- issue:#360 date:2026-06-13 expires:2026-12-13 source:code-review path:backend/tests/utils/ -->
- [AVOID] `PreMarketScanMissedSlot` joins on `markethawk_run_frequency_seconds` which is never exported; export that gauge (or drop the join and threshold on absolute staleness) or the alert can never fire. <!-- issue:#391 date:2026-06-15 expires:2026-12-15 source:code-review path:prometheus/ -->
- [AVOID] In gate_metadata dicts threaded to save_event(), use 'tier' (not 'verdict') as the trust-level key — stats.py reads metadata_["quality_gate"]["tier"] and silently ignores the field if 'verdict' is used instead <!-- issue:#494 date:2026-06-22 expires:2026-12-22 source:conformance path:backend/app/tasks/ -->
- [AVOID] Before declaring historical quantitative data (tokens/duration) 'not measurable,' check whether entrypoint.sh's post_cost_report() already posts a durable per-node cost-report comment (marker  dark-factory-cost-report ) to the issue — it does, since 2026-05-27, and is minable the same way conformance/code-review verdicts are mined from issue comments. <!-- issue:#48 date:2026-07-13 expires:2027-01-13 source:conformance agent:conformance scope:codebase path:evals/ -->
- [AVOID] The added test pins the rollout runbook to `docs/superpowers/specs/2026-07-13-roll-out-dark-factory-claude-skills-design.md` and asserts it is "not archived", but a later commit on this same branch (`e771def docs: archive spec/plan for issue #49`) moved that file into `docs/archive/`. 5 of these tests now fail against HEAD, so `python -m pytest tests/` (exactly what CI runs) goes red and blocks the PR. This is the CLAUDE.md "never archive a doc that tests or README still reference" rule being violated. Fix by restoring the spec to `docs/superpowers/specs/` (revert the archive move) — not by loosening the test. <!-- issue:#49 date:2026-07-13 expires:2027-01-13 source:code-review agent:code-review scope:codebase path:tests/ -->
- [AVOID] A prior spike's merge commit message can claim deliverables (e.g. #661's message claimed a spec file at docs/superpowers/specs/2026-06-27-agentmemory-memory-backend-spike.md and a .archon/memory AVOID entry) that were never actually committed to the repo — only scripts/eval_agentmemory.sh and scripts/iii-config.agentmemory.yaml survived from that spike. Before citing a prior ticket's outcome as precedent, verify claimed artifacts actually exist (git show --stat <commit>, grep the working tree) rather than trusting the commit message's stated scope. <!-- issue:#50 date:2026-07-17 expires:2027-01-17 source:refine agent:refine scope:codebase path:docs/superpowers/specs/ -->

---
<!-- PROVISIONAL — entries below are from a single observed run; unverified.
     Do not rely on these as authoritative guidance. They are excluded from
     plan/implement prompt injection except as advisory context.
     Each will be promoted to [PATTERN] on second-run confirmation (different issue number) or dropped at TTL. -->

- [PROVISIONAL] One-shot `ruff check --fix` cleanup commits must use `git commit --no-verify` because `.pre-commit-config.yaml` registers a `ruff-format` hook that fires on `backend/` files and fails on unfixed formatting; `ruff format` is a separate operation from `ruff check --fix`. <!-- evidence:pre-commit-hook-output issue:#285 date:2026-06-11 expires:2026-12-11 source:implement -->
- [PATTERN] Define service-level `typing.Protocol`s in `schemas/` (not a new `protocols/` folder) — this is the established contract home for the domain. Export a module-level singleton typed as the Protocol from the concrete service file (`quality_gate_service: QualityGateServiceProtocol = QualityGateService()`); consumers import and call the singleton rather than the class so adding new implementations never requires editing consumers. <!-- issue:#633 date:2026-06-27 expires:2026-12-27 source:implement -->
- [PATTERN] When monkeypatching a per-instance seam method (e.g. `_request`/`_run`) as a class attribute from a fixture controller, use a plain closure `def _request(instance, ...): return controller._dispatch(...)`, never a bound method (`self._request`) — a bound method is not a descriptor so `monkeypatch.setattr(Cls, "name", self.bound_method)` never re-binds to the instance on attribute access, silently swallowing the instance as the first positional arg (`TypeError: missing 1 required positional argument`). See `tests/test_tracker_contract.py`'s `_JiraFixtureController.__init__`. <!-- issue:#251 date:2026-07-13 expires:2027-01-13 source:implement -->
- [PATTERN] The #250 two-dot entry above is scoped to single-file content-equality checks (is this one file's content already identical to main's tip). For changed-file-SET detection — deciding which files a branch itself touched, e.g. `oos_excise.sh`'s OOS scan — use three-dot (`origin/main...HEAD`, merge-base semantics), matching every other gate command in this repo. Using two-dot for set detection flags/deletes files `main` changed independently after the branch forked; this caused `oos_excise.sh` to delete `scripts/factory_core/providers/*` on the #251 branch. <!-- issue:#266 date:2026-07-14 expires:2027-01-14 source:implement -->
- [FIX] A hand-written bash test that sets `SCHEDULER_STATE_DIR=$(mktemp -d ...)` before invoking `cli.py run-record record|assemble`/`error-signature-write` as a python3 subprocess must `export SCHEDULER_STATE_DIR` (plain assignment is not enough) — the subprocess reads it via `os.environ.get(...)`, so an unexported var silently falls through to the real `/var/lib/dark-factory` default. This does not affect entrypoint.sh itself in production (Docker `environment:` vars are already OS-level exported when the container process starts), only ad hoc test scripts that set the var as a fresh local shell variable. <!-- issue:#300 date:2026-07-18 expires:2027-01-18 source:implement -->
- [PATTERN] A static source-text guard test (greps another file's text but never sources/executes it, e.g. tests/test_cost_report_harness_economics.sh) can share command-name substrings with real invocation sites in its own comments/echo strings. A hermeticity/lint script that greps tests/*.sh for a command name (e.g. `run-record assemble`) to flag missing env-var isolation must additionally require evidence of actual execution (e.g. `ENTRYPOINT_SOURCE_ONLY=1 source` or a literal `cli\.py` invocation) before flagging — otherwise it false-positives on prose mentions. <!-- issue:#300 date:2026-07-18 expires:2027-01-18 source:implement -->
- [PATTERN] When implementing an approved plan whose code blocks are literal snapshots of the target file, diff against the CURRENT file before pasting — plans can drift after approval (issue #185: `has_new_comment_after_report`'s bot-match logic and the CI-gate/orphan-sweep/plan/refine comment footers had all switched from hardcoded strings to `$BOT_RE`/`FOOTER=$(python3 ... marker scheduler)` via an intervening #181 fix landed between plan approval and implementation, ~1 day later). Extract/move the *current* file's actual content, not the plan's embedded snapshot. <!-- issue:#185 date:2026-07-24 expires:2027-01-24 source:implement -->
- [PATTERN] When extracting functions out of scheduler.sh into a new sourceable lib (or any similar move), grep for structural/source-text tests that awk/grep the ORIGINAL file directly (e.g. tests/test_scheduler_ceiling.sh, which extracts function bodies from scheduler.sh via `awk "/^fn_name()/{f=1}..."`) — these are invisible to SCHEDULER_SOURCE_ONLY=1 sourcing-based regression tests (which exercise the sourced function, not raw text) and silently break when the function moves to a different file. Repoint the grep target at the new file as part of the same extraction commit. <!-- issue:#185 date:2026-07-24 expires:2027-01-24 source:implement -->
- [PROVISIONAL] `tests/test_dispatch_ceiling.sh` (S1 unbound `STATUS_READY` var + a `factory-core ... marker ...` subcommand that `factory_core/cli.py`'s current argparse choices no longer include), `tests/test_159_regression.sh` (2 stub/assert-mismatch failures), and `tests/test_identity.sh` ("FAIL status ids") all fail on `origin/main` HEAD as of 2026-07-24, unrelated to any in-flight change — verified by running each against a bare `origin/main` worktree. A "full regression pass" task instruction naming these files should not assume green-before-your-change; diff-check against main first rather than debugging them as newly introduced. <!-- evidence:test-output issue:#185 date:2026-07-24 expires:2027-01-24 source:implement -->
- [PATTERN] Before implementing on a fresh `intent=new` dispatch, check `git log --oneline origin/<current-branch>` against local `HEAD` — a prior orphaned run can already have pushed a complete, conformance-passed implementation to the same branch name while only its `push-and-pr` step failed (e.g. issue #185: local checkout was reset to bare `main`, but `origin/feat/issue-185-...` was 9 commits ahead with the finished, tested work). If local has zero unique commits vs `origin/<branch>`, `git merge --ff-only origin/<branch>` to adopt the prior work and re-verify tests, instead of re-implementing from the issue description — re-implementing risks diverging from already-conformance-approved code and wastes the run's budget. <!-- issue:#185 date:2026-07-24 expires:2027-01-24 source:implement -->
